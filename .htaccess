# Bloquear listado de directorios en todo el sitio
Options -Indexes

# Bloquear acceso por URL con path traversal (../)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} \.\. [OR]
    RewriteCond %{REQUEST_URI} \.\./
    RewriteRule ^ - [F,L]
</IfModule>

# No afectar la carpeta flutter (usada por el dispositivo)
# Las reglas anteriores no bloquean flutter; solo evitan listado y ".." en la URL
